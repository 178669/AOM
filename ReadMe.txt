If using a Jetson Nano 2GB ram, run GUI_LowMem.py, which is optimized for
memory using real-time frame generation and utilizing the Jetson's dedicated graphics processor.

Otherwise, on devices with greater than 2GB ram like some Raspberry Pi configurations,
please use GUI_LowGPU.py